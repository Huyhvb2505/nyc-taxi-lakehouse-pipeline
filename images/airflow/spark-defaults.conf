# Defaults applied to jobs submitted from Airflow (client mode: the driver runs
# inside the airflow container, executors run on the Spark workers).
# Both Delta and Iceberg are available.
spark.master                       spark://spark-master:7077
spark.submit.deployMode            client
spark.driver.host                  airflow

spark.sql.extensions               io.delta.sql.DeltaSparkSessionExtension,org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions

# --- Delta (default catalog) ---
spark.sql.catalog.spark_catalog    org.apache.spark.sql.delta.catalog.DeltaCatalog

# --- Iceberg (named catalog: iceberg.<db>.<table>) ---
spark.sql.catalog.iceberg          org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.iceberg.type     hadoop
spark.sql.catalog.iceberg.warehouse file:///data/lakehouse/iceberg

spark.sql.warehouse.dir            file:///data/lakehouse
